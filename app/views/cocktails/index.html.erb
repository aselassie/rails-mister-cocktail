<!-- <div class="jumbotron"> -->

    <!-- <h1>List All Cocktails</h1>
    <% @cocktails.each do |cocktail| %>
      <h3><i class="fas fa-cocktail"></i> <%= cocktail.name.capitalize %>  <%= link_to "Choose ingredients", cocktail_path(cocktail), class: "btn btn-outline-info" %> </h3>
      <% end %>
      <%= link_to "Create new", new_cocktail_path, class: "btn btn-outline-dark" %> -->
  <!-- </div> -->

<div class="container">
<div class="row">
  <% @cocktails.each do |cocktail| %>
    <div lass="col-12 col-md-6 col-lg-3">
      <div class="card" style="background-image: url('https://source.unsplash.com/500x400/?cocktail+<%= cocktail.name %>')">
        <div class="card-body">
          <%= link_to cocktail_path(cocktail), class: "btn btn-info" do %>
            <%= cocktail.name.capitalize %>
          <% end %>
        </div>
      </div>
    </div>
  <% end %>


   <%= link_to "Create new", new_cocktail_path, class: "btn btn-outline-dark" %>

</div>
